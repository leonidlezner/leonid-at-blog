---
import Base from "../layouts/Base.astro";

interface Props {
  title: string;
  area?: string;
}

const { title, area } = Astro.props;
---

<Base title={title} area={area}>
  <div class="container mx-auto max-w-3xl my-10">
    <slot />
  </div>
</Base>
